# `data` is available as builtin and is a dictionary with the input data.  # noqa: D100
# name = data.get("name", "world")
# `logger` and `time` are available as builtin without the need of explicit import.
# logger.info(f"Received name: {name}")
# logger.info(f"Hello {name} at {time.time()}")
